@model List<Not>
@{
}


<ul class="list-group">
  @foreach (var item in Model)
  {
    <li class="list-group-item list-group-item-@item.Renk">@item.Tarih.ToString("dd.MM.yyyy") - @item.Detay</li>
  }
</ul>
